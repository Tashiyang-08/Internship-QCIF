# --- API origins
BACKEND_URL=http://127.0.0.1:8000
FRONTEND_URL=http://localhost:5173

# --- Auth/JWT (use one key; keep stable across restarts)
JWT_SECRET=change_me_long_random_but_stable
JWT_ALG=HS256
VERIFY_TOKEN_EXPIRES_MIN=60
RESET_TOKEN_EXPIRES_MIN=30

# --- ASR defaults
FASTER_WHISPER_VAD=0
FASTER_WHISPER_MODEL=medium
FASTER_WHISPER_DEVICE=cpu
FASTER_WHISPER_COMPUTE_TYPE=int8

# --- Database (PERSISTENT)
# EITHER (recommended) absolute path:
DATABASE_URL=sqlite:///C:/Users/tashi/Desktop/STT/asr_backend_data.sqlite3
# OR relative path (creates next to server process):
# DATABASE_URL=sqlite:///./asr_backend_data.sqlite3

# Back-compat for code that still reads SQLITE_URL:
SQLITE_URL=${DATABASE_URL}
